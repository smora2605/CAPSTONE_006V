import 'package:flutter/material.dart';

class AppointmentManagement extends StatelessWidget {
  const AppointmentManagement({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('AppointmentManagement'),
    );
  }
}